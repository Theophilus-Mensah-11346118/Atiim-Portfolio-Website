import React from 'react';
import Header from '../sections/Header';
import Footer from '../sections/Footer';

const Portfolio = () => {
  return (
    <div>
      <Header />
      <section>
        <h1>Portfolio page</h1>
        <form>
          {/* Add form fields based on the PDF */}
        </form>
      </section>
      <Footer />
    </div>
  );
};

export default Portfolio;
